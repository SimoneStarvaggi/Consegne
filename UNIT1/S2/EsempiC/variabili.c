#include <stdio.h>

int main(){
    int intero = 1; // un intero a 4 byte, ovvero 32bit -> %d
    char carattere = 'c'; // un carattere di 1 byte, ovvero 8bit -> %c
    short corto; // un numero intero di 2 byte, ovvero 16bit -> %hd
    long lungo; // un numero intero di 8 byte, ovvero 64bit -> %ld
    float numeroreale; // un numero reale di 4 byte, ovvero 16bit + 16 bit per i decimali -> %f
    double numeroreale; // un numero reale d 8 bite, ovvero 32bit + 32 bit per i decimali -> %lf


    /*
    Curiosita' su float
    %0.1f = float con arrotondamento 1 = 1.897281 -> 1.9
    */
}