#include <stdio.h>

int main()
{
    // int numeri[5]; // senza inizializzare valori
    int numeri[5] = {0}; // azzera tutti i valori
    numeri[0] = 1; // per assegnare un valore var[indice] = valore
    numeri[1] = 2;
    int numerib[] = {1, 2, 3, 4, 5}; // si puo' inizializzare con i valori
    float decimali[] = {1.5, 2.75, 3.14};
}